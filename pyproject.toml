[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "uma_theme"
version = "1.0.0"

[tool.setuptools.packages.find]
where = ["."]
include = ["uma_theme*"]
